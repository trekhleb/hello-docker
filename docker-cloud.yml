web:
  # Replace username/repo:tag with your name and image details.
  image: 'trekhleb/hello-docker:latest'
  autoredeploy: true
  ports:
    - '80:80'
  links:
    - redis
redis:
  image: 'redis:latest'
